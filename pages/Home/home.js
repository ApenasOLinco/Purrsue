const gatoInfos = document.querySelectorAll('.gato-info-row');

if (gatoInfos.length > 0) {
    gatoInfos.forEach(row => {
        row.addEventListener('click', () => {
            
        });
    });
}